/**
 * Example client hook pattern: prefer TanStack Query for server state.
 *
 * Usage after API routes exist:
 *   const q = useHealthQuery();
 */
export function usePlaceholderHook(): void {
  // Intentionally empty — replace with real hooks per feature module.
}
